import 'package:flutter/material.dart';

/// Dubai desert-at-dusk palette. Calm editorial luxury.
class AppColors {
  AppColors._();

  // Primary
  static const primary = Color(0xFF0E7C7B);
  static const primaryDark = Color(0xFF0A5C5B);
  static const primaryLight = Color(0xFFD4EFEF);

  // Accent (warm amber — CTAs, locked highlights, sparingly)
  static const accent = Color(0xFFE8A33D);
  static const accentLight = Color(0xFFFFF3E0);

  // Ink & text
  static const ink = Color(0xFF14201F);
  static const muted = Color(0xFF5B6B6A);
  static const subtle = Color(0xFF8E9E9D);

  // Surfaces
  static const surface = Color(0xFFFBF9F4); // warm off-white
  static const card = Color(0xFFFFFFFF);
  static const divider = Color(0xFFE8E5E0);

  // Semantic
  static const success = Color(0xFF2E7D5B);
  static const warning = Color(0xFFC77D1A);
  static const danger = Color(0xFFB3402F);

  // Sponsored tag (subtle, never loud)
  static const sponsoredBg = Color(0xFFEFE7D6);
  static const sponsoredText = Color(0xFF8B6914);

  // Dark mode variants
  static const darkSurface = Color(0xFF1A2625);
  static const darkCard = Color(0xFF223332);
  static const darkInk = Color(0xFFF0F0EC);
  static const darkMuted = Color(0xFFA0B0AF);
}
